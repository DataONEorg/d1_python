<?xml version='1.0' encoding='UTF-8'?>
<replicas>
  {% for pid, replicaMemberNode, replicationStatus, replicaVerified in status_list %}
  <replica>
	  <pid>{{ pid }}</pid>
	  <replicaMemberNode>{{ replicaMemberNode }}</replicaMemberNode>
	  <replicationStatus>{{ replicationStatus }}</replicationStatus>
	  <replicaVerified>{{ replicaVerified }}</replicaVerified>
  </replica>
  {% endfor %}
</replicas>
